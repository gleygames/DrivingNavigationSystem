# Implementation Progress

Mark a step `[x]` and add the date **only after the user confirms** its tests and manual checks passed.

`[HARD]` = recommended Claude Opus 5, or Claude Sonnet 5 at xhigh effort. The agent stops and asks before starting these.

## Phase 1 — Foundation
- [x] S01 Assemblies and test setup (2026-09-18)
- [x] S02 Dev sandbox city and test car (2026-09-18)

## Phase 2 — Rendering spike
- [x] S03 Route line mesh builder (2026-09-18)
- [x] S04 [HARD] Route line shader, graphic and Route Line Lab (2026-09-21)
- [x] S05 Route line chunks (2026-09-21)

## Phase 3 — Core data
- [x] S06 Navigation Settings asset (2026-09-21)
- [x] S07 Coordinate conversions and floating origin shift (2026-09-21)
- [x] S08 Road network runtime data and builder (2026-09-21)
- [x] S09 Road spatial grid (2026-09-21)
- [x] S10 Map asset and data format versions (2026-09-21)

## Phase 4 — Pathfinding
- [x] S11 Route types and snapping (2026-09-21)
- [x] S12 [HARD] A* search (2026-09-21)
- [x] S13 [HARD] Mid-road start and destination (2026-09-21)
- [x] S14 Zero garbage and performance on a large network (2026-09-21)

## Phase 5 — Authoring data and bake
- [x] S15 Authoring asset (2026-09-21)
- [x] S16 Curves, adaptive sampling and ground probe (2026-09-21)
- [x] S17 Edit operations (2026-09-21)
- [x] S18 Bake and outdated detection (2026-09-21)
- [x] S19 Validation (2026-09-21)
- [x] S20 Format versions and migrations (2026-09-21)
- [x] S21 Importer interface and source replacement (2026-09-21)

## Phase 6 — Editor tools
- [x] S22 Map scene object, rectangle handles, sync and overlay (2026-09-21)
- [x] S23 Navigation window shell and asset locations (2026-09-21)
- [x] S24 Scene view road drawing (2026-09-21)
- [x] S25 [HARD] Draw mode (2026-09-21)
- [x] S26 [HARD] Edit and Connect modes (2026-09-21)
- [x] S27 Validate and Bake modes, Play-mode warning, build check (2026-09-21)
- [x] S28 Project settings panel (2026-09-21)
- [x] S29 Large network editor check (decision point: chunking) (2026-09-21)

## Phase 7 — Map image capture
- [x] S30 Capture planner (2026-09-21)
- [x] S31 [HARD] Capture execution, pipeline adapters and import settings (2026-09-21)
- [x] S32 Custom images: ratio lock, guidance and template export (2026-09-22)

## Phase 8 — Runtime navigation
- [x] S33 Vehicle motion (2026-09-22)
- [x] S34 [HARD] Road matching (2026-09-22)
- [x] S35 Navigation session (2026-09-22)
- [x] S36 Reroute rules (2026-09-22)
- [x] S37 [HARD] Navigation Manager: core loop (2026-09-22)
- [x] S38 [HARD] Navigation Manager: API, preview flow, events and command queue (2026-09-22)
- [x] S39 Formatter and text adapters (2026-09-22)
- [x] S40 Navigation Events component (2026-09-22)
- [x] S41 Floating origin misconfiguration warning (2026-09-22)

## Phase 9 — Map views
- [x] S42 Map View core (2026-09-22)
- [x] S43 Route display in views (2026-09-22)
- [x] S44 Follow Car behavior (2026-09-22)
- [x] S45 Minimap shape, compass, tap to open (2026-09-22)

## Phase 10 — Markers
- [ ] S46 Marker component, registry and grid
- [ ] S47 Marker layer in views
- [ ] S48 Off-screen arrows

## Phase 11 — Full map interaction
- [ ] S49 Interactive behavior
- [ ] S50 Pointer adapter and gestures
- [ ] S51 Tapping, preview panel and buttons
- [ ] S52 Crosshair mode and the Input System adapter

## Phase 12 — Setup and finish
- [ ] S53 Default prefabs, placeholder art, safe area, default assets
- [ ] S54 Setup window
- [ ] S55 Enter Play Mode Options audit
- [ ] S56 Performance pass

## Implementation notes

Added by the implementing agent when a step passes: anything later steps need to know that differs from the plan or isn't obvious from the code. Format: `Sxx: <note>`.

- S01: `Runtime/AssemblyInfo.cs` and `Editor/AssemblyInfo.cs` had no `using System.Runtime.CompilerServices;` line before this step; it was added along with the `InternalsVisibleTo` attributes.
- S02: In `Dev.Editor`, `System.Random` must be fully qualified (`new System.Random(...)`) — a bare `Random` is ambiguous with `UnityEngine.Random` once both `using System;` and `using UnityEngine;` are present. `SandboxSceneBuilderTests` uses `EditorSceneManager.NewScene(..., NewSceneMode.Single)`, not `Additive` as in the plan — `Additive` throws `InvalidOperationException` in the Test Runner because the active untitled scene isn't saved. `SandboxSceneBuilder` exposes `RoadObjects` (`IReadOnlyList<GameObject>`) and `ExpectedRoadObjectCount` (const, = 18) for tests/later steps to check road-segment output; no layer named `Road` exists yet so road segments currently sit on layer `Default`.
- S03: `RouteLineMeshBuilder.Build` groups vertex pairs internally (one pair per "join point": a plain point, or two pairs for a bevel or a dashed-flag change) and emits a quad between every consecutive pair of groups — this is how the plan's separate bevel/dashed-duplicate rules and the "two triangles per quad" rule combine. When coincident points are skipped (closer than 0.0001 m), the kept segment's dashed flag is taken from the last original segment leading into the next kept point (the plan doesn't specify this interaction).
- S06: `NavigationSettings.AddRoadType` and `RemoveRoadType` bump `version` too (not just `SetRoadTypeSpeed`/`Width`) — the field's own doc comment and the design's cross-cutting section both say add/remove affects bakes. `MoveRoadType` is the only mutator that does not bump it. No `SetUnitsPerMeter` or channel-name setter methods exist yet; editor code should edit those serialized fields directly via `SerializedProperty` until a settings panel (S28) needs otherwise.
- S07: `WorldConverter` defaults to `unitsPerMeter = 1` on construction (not specified in the plan; nothing wires it from `NavigationSettings` yet). `MapFrame.TrueToMap`/`MapToTrue` use the rotation convention: `localX = relX*cos - relZ*sin`, `localZ = relX*sin + relZ*cos` (Unity's standard Y-axis rotation matrix), verified against all worked examples in the plan; later steps needing map rotation math should reuse `MapFrame` rather than re-deriving it. Testing a `CustomLogger.LogError` call (e.g. `WorldConverter.SetUnitsPerMeter`'s guard) needs `UnityEngine.TestTools.LogAssert.ignoreFailingMessages = true` around the call, since `LogError` has no `[Conditional]` gate and would otherwise fail the test — no prior test in the repo needed this pattern.
- S05: `RouteLineRenderer` only forwards style (colors/widths) to a newly pooled `RouteLineGraphic` once `SetStyle` has been called at least once on the renderer (`_hasStyle` flag) — otherwise a graphic created for a longer route would get zeroed-out C# default colors/widths instead of `RouteLineGraphic`'s own built-in defaults. Trim distance and canvas-units-per-meter are always forwarded to new graphics since their 0/1 defaults already match. `SetShader` is only forwarded to graphics (existing or newly created) when a non-null shader was actually set on the renderer, so pooled graphics fall back to `RouteLineGraphic`'s own `Shader.Find` default. Pooled chunk graphics are disabled via `gameObject.SetActive(false)`, never destroyed, and get a full-stretch `RectTransform` (anchors 0,0–1,1, zero offsets) so every chunk shares the same local origin.
- S09: `RoadGrid.GetCellRange(minX, minZ, maxX, maxZ, ...)` is the shared, public helper that turns a world-space bounding box into a clamped cell-coordinate range (min and max clamped independently to `[0, count-1]` on each axis, since clamping is monotonic and preserves `min <= max`); both `RoadGrid.Build` (segment bounding boxes) and `RoadQuery.FindNearest` (query circle) use it, and later grid consumers should reuse it too rather than re-deriving the clamp math (an earlier version that clamped only one side per bound produced an out-of-range cell index when a query fell entirely outside the grid on one axis). `RoadGrid.DefaultCellSize` (= 50f) is the one place the 50 m default lives; `RoadNetworkBuilder.Build`'s new `cellSize` parameter defaults to it.
- S10: `CurrentFormatVersion` stays a `const` on `MapData`, `RoadNetworkData` and `NavigationSettings`; `IFormatVersioned.CurrentFormatVersion` is implemented explicitly (`int IFormatVersioned.CurrentFormatVersion`) on each so it coexists with the const of the same name — access it through an `IFormatVersioned` reference, not the concrete type. Added `MapData.SetFormatVersion` (not in the plan's file list) since migrations (S20) will need it.
- S11: `RouteSnapper` is constructed with a `RoadNetworkData` (builds its own internal `RoadQuery`), not a pre-built `RoadQuery` — simpler for both `Pathfinder` and tests to construct. `Route`'s Pathfinder-filled properties (`Network`, `Failure`, `Start`, `End`, `Destination`, `Length`, `Eta`, `Success`, `ArrivedImmediately`) use `internal set` — S12/S13 set them directly rather than through mutator methods; `Network` is `internal`. `RoutePreferences.SetPreference` removes a road type's entry from its parallel lists when set back to `Normal` (keeps them compact instead of accumulating `Normal` entries). `Route.GetTruePoints` uses one general "cut at any distance" interpolation path for every segment (no separate full-road-vs-partial branch) — later steps needing route polylines should call it as-is rather than re-deriving the interpolation.
- S12: `Pathfinder` is built from reusable pieces S13 should extend rather than rewrite: `BeginSearch(prefs)` (stamp, per-road `costPerMeter`, `minCostPerMeter`), `AddStartState(state, g)` (initial states; parent −1), `EvaluateFinish(intersection, state, g)` (finish candidates against `destinationRoad`/`destinationDistance`/`destinationPoint`), `RunSearch()`, `WriteResult(route)`. Extra internal test entry `FindRouteFromRoad(startRoad, forward, toIntersection, prefs, result)` (approved plan change): a single fixed-direction start state, its road included as the first segment — the U-turn/dead-end tests need it because with an intersection start U-turns are never on a shortest path. Finish rules: a finish piece of length 0 is always legal and appends no segment (so an intersection destination is reached via any arriving road); non-zero finish pieces respect one-way and the U-turn rule. The start intersection is evaluated as a finish with g = 0 (from == to → success, no segments). `Route.Start`/`End` are not filled yet (S13). Added `RoutePreferences.GetSmallestMultiplier()` (internal; min of 1 and every configured type's multiplier) for the heuristic.
- S13: `Pathfinder` now owns a `RouteSnapper` (created in its constructor). `FindRoute` fills `Route.Start`/`End` as soon as both snaps succeed (also on `NoPath`); `Destination` is always `request.To`. The finish candidate stores `bestFinishFrom` (0/Length for intersection arrivals, the start distance for the direct same-road piece), and the path's first state is cut at `startDistance` when `hasStartPoint` is set. Zero-length start pieces (start snapped exactly onto an intersection) are not emitted as segments. The heuristic targets the snapped destination position.
- S14: `Pathfinder.FindRoute` needed no changes — its arrays are all preallocated in the constructor and reused per call, `Route.Clear`/`AddSegment` reuse the internal list, and `RoadRecord`/`RoadPoint` are structs, so it was already allocation-free. `TestCityGenerator.Generate` builds a jittered grid (cell 100 m, ±20 m jitter) plus diagonal roads (~12% of cells) sized from `approximateRoadCount` via a fixed roads-per-cell estimate; for 5000 it produced 5176 roads on the measured run, well inside 4500–5500. Measured average on the dev machine: 1.225 ms/request over 200 pairs on a 5176-road network (target ≤ 2 ms; design's 5 ms low-end-phone target has much more headroom).
- S15: `AuthoringRoad.KeyPoints`/`Points`/`GroundMissIndices` and `RoadNetworkAuthoring.Roads`/`Intersections` are directly mutable `List<T>` properties (no separate add/remove methods) since S16–S19 need index-level manipulation (insert, split, reverse); `Roads`/`Intersections` on `RoadNetworkAuthoring` are `internal`, matching `MapData`'s pattern of `internal` setters for state only editor tooling code should touch. `RuntimeAsset`, `Settings` and `MapAsset` have no setters yet — editor code should assign them via `SerializedProperty` (as `NavigationSettings.UnitsPerMeter` does per the S06 note) until a step that needs a C# setter adds one; S18's Bake will need one for `RuntimeAsset` when it creates the runtime asset.
- S16: `RoadCurve.Split` does not mutate `AuthoringRoad`; it returns the new mid `AuthoringKeyPoint` (with `manualHandles = true`) and, via `out` params, the corrected `newStartOutHandle`/`newEndInHandle` for the start/end key points — the caller (S17's `InsertKeyPoint`/`SplitRoad`) applies those and inserts the returned key point itself. Pure binary subdivision (bisect at the parametric midpoint, explicit stack) only ever produces power-of-two segment counts per Bézier segment; for a straight 100 m road at the design's default 20 m max spacing this yields 8 segments of 12.5 m (9 points), not the plan's illustrative "6 points" (5×20 m) — spacing and deviation bounds are still fully respected, later steps should expect point counts at this granularity rather than exactly `ceil(length/maxSpacing)`. `RoadSampler.Sample` clears and refills `road.Points`/`road.GroundMissIndices` each call and skips a segment's first point when `segment > 0` (it's identical to the previous segment's last point) — S17 operations that re-sample a single edited road should call `Sample` per affected road as usual, no extra dedup needed. `PhysicsGroundProbe` builds its own internal `WorldConverter` (shift always zero) from the `unitsPerMeter` passed to its constructor; it does not read `NavigationSettings` itself, so callers (S22+ editor tools) must pass `settings.UnitsPerMeter` in.
- S17: `InsertKeyPoint` and `SplitRoad` mark the two key points bordering a cut as `manualHandles = true` (in addition to the new split point, which `RoadCurve.Split` already marks manual) — every operation's generic re-sample step (`UpdateAutoHandles` then `Sample`) would otherwise immediately recompute those points' handles from neighbor distance and silently distort the exact split shape. `MergeAtIntersection` rebuilds the surviving road's key-point list from scratch (concatenating whichever road ends at the intersection first, reversing one side with a new `ReverseKeyPoints` helper — shared with `Flip` — when a two-way pair meets end-to-end or start-to-start); the surviving `AuthoringRoad` keeps its ID but gets new outer intersection IDs and a rebuilt `KeyPoints` list. `ConnectToRoadMiddle(intersectionId, roadId, segment, t)` removes `intersectionId` (per `ConnectIntersections`' `fromId`/`intoId` contract) — later code that needs the merged intersection after this call should read it off the affected road's `Start`/`EndIntersectionId`, not assume `intersectionId` still exists.
- S18: `RoadNetworkAuthoring` got an `internal SetRuntimeAsset` and `RoadNetworkData` got `internal SetSourceVersion`/`SetFormatVersion` (same pattern as `MapData`'s existing field setters) so `RoadBaker` can wire and stamp them — neither existed before this step. `RoadBaker.Bake` derives the runtime asset's path from `AssetDatabase.GetAssetPath(authoring)` by string surgery (replacing a trailing `_RoadsAuthoring` with `_RoadsRuntime`, or appending `_RoadsRuntime` if the authoring file doesn't follow that naming), so the authoring asset must already be saved on disk before baking (in-memory-only authoring assets have no path and baking would produce an invalid path). `Bake` also calls `EditorUtility.SetDirty` on the authoring asset itself (not just the runtime asset) since assigning a freshly created `RuntimeAsset` reference is a change to authoring's own serialized data.
- S19: `RoadValidator`'s near-miss distance (2 m) and duplicate tolerance (1 m) are `public const float` on the class (`DefaultNearMissDistance`, `DefaultDuplicateTolerance`) — `NavigationSettings` has no such fields, same pattern as `RoadGrid.DefaultCellSize`. `CheckIslands` and `CheckOneWayTraps` each independently rebuild their own intersection id→index map and adjacency (no shared field/cache) since the class is stateless; cheap enough even on the 5000-road stress test. `NearMiss` and `OutsideMap` stop at the first offending match per intersection/road (one issue each), matching `GroundMiss`'s one-per-road granularity — not one issue per point/pair. `Editor/Authoring/AuthoringFromBuildInput.cs` (new, Dev.Editor, per the plan) turns each `BuildRoad.Points` entry into both an `AuthoringKeyPoint` (auto handles via `RoadCurve.UpdateAutoHandles`) and the dense `AuthoringRoad.Points` directly (no `RoadSampler` resample) since the generator's points are already dense; later steps reusing it for large-network tests should expect `Points` to equal the input's points verbatim, not a re-sampled curve.
- S20: `FormatMigrator.FindMigration` matches on exact `asset.GetType()` against `IFormatMigration.AssetType`, not a base-type or interface check — a migration is only picked up for the concrete versioned type it targets. Added `internal SetFormatVersion` to `NavigationSettings` and `RoadNetworkAuthoring` (matching the setter `MapData`/`RoadNetworkData` already had from S10/S18); any later versioned asset type will need the same. Watch for `Object` ambiguity (`UnityEngine.Object` vs `System.Object`) when a file has both `using System;` and `using UnityEngine;` — write `UnityEngine.Object` explicitly instead of the bare name.
- S04: `RouteLineGraphic` must carry `[RequireComponent(typeof(CanvasRenderer))]` — in uGUI 1.0 the base `Graphic` doesn't require it, and without it every rebuild is silently skipped (applies to any future custom `Graphic`). Per the S04 decision, the shader widens in canvas space: hidden property `_CanvasOffsetMatrix` (float4 = 2x2 graphic-to-root-canvas rotation with unit-length columns; uGUI hands nested-canvas vertices to the shader in root canvas space, so HalfWidth/OutlineWidth are root canvas units — corrected 2026-09-22, see S43 note) is computed by `RouteLineGraphic.UpdateRouteLineVisuals(deltaTime)`, driven by `Canvas.willRenderCanvases`, and set on the materials only when it changes; `_CanvasUnitsPerMeter` is used only for dashes. Internal `CanvasOffsetMatrix` property exposed for tests. The graphic also adds TexCoord1/2 to the root canvas when nested. `raycastTarget` is set false once in `Awake` (hidden serialized `defaultsApplied` flag). The material instance is `HideAndDontSave` and is recreated on enable from the serialized shader. PlayMode tests: a freshly created Overlay canvas rebuilds child graphics once while it settles, so build-count tests use `[UnitySetUp]` with two settle frames, compare against a baseline, and wait with `yield return null` + `Canvas.ForceUpdateCanvases()`.

- S22: Automated tests (`MapRectangleSyncTests`, 7/7) passed 2026-09-21 and the step is marked done so S23 (which depends on it) isn't blocked. Its 3 manual checks (corner drag, Move/Rotate sync, locked-revert notification) were **not run** — `MapData` has no `[CreateAssetMenu]` (by design, it's meant to be created by S23's `NavigationAssetLocator`/setup tooling), and creating one by hand via Unity's generic "Create > Scriptable Object" wizard was judged not worth the detour before that tooling exists. User will run these manual checks once S23 gives an easy way to create a `MapData` asset — do that together with S23's manual checks rather than assuming S22's Scene view behavior (corner handles, transform sync, lock revert) is confirmed working yet.

- S21: `RoadEditOperations.CreateRoad` never sets `SourceTag` (it has no parameter for it); `RoadImportContext.AddRoad` sets it via the internal `AuthoringRoad.SetSourceTag` right after `CreateRoad` returns, so the road's first `FinishRoad` (inside `CreateRoad`) sees an empty tag and `modifiedAfterImport` correctly starts `false`. Neither `RoadImportContext` nor `RoadImportRunner` had constructor signatures in the plan; `RoadImportContext(asset, operations, sourceTag, mergeDistance)` and `RoadImportRunner(probe, maxDeviation, maxSpacing, mergeDistance)` follow `RoadEditOperations`'s own constructor pattern — a runner/context configured once, `Run(importer, asset)` buildable against any authoring asset. `AddRoad` calls the public `FindOrCreateIntersection` internally (using the constructor's `mergeDistance`) to resolve both endpoints, which is what lets chained imported roads share an intersection automatically; later import adapters (Traffic System, etc.) should rely on this rather than resolving intersections themselves. `RoadImportRunner.Run` reuses `RoadEditOperations.DeleteRoad` per matching road for the "replace this source" step, so orphaned intersections are cleaned up for free.

- S24: `RoadDrawPlanner.Plan(authoring, unitsPerMeter, frustum, cameraPosition, detailDistance, typeFilter, visibleRoadIds, detailed)` caches per-road `Bounds` in **True** space keyed by `AuthoringRoad.Id` (rebuilt on `authoring.Version` change) and converts to World only at `Plan`-time via `unitsPerMeter` (shift is always zero in editor code, same pattern as S16's ground probe); type filter is a `HashSet<int>` of type IDs, empty = show all. `RoadSceneDrawer.Draw` looks roads up by `authoring.FindRoad(id)` from the `visibleRoadIds` list — fine at current scale, revisit if S29 finds it slow. `NavigationWindow` owns `drawPlanner`/`sceneDrawer`/`editorPrefs`/`typeFilter`/`visibleRoadIds`/`detailedRoads`/`validationIssues`/`frustumPlanes` and calls `DrawRoadNetwork(sceneView)` at the top of `HandleSceneGUI`, before the current mode's own `OnSceneGUI` — mode drawing (S25+) paints on top of the network. `RoadSceneDrawer.Draw` takes `authoringAsset`'s `validationIssues` as a `List<ValidationIssue>`; `NavigationWindow` currently always passes its own empty list since `ValidateMode` (S27) doesn't produce results yet — S27 should fill that same list (or wire its own) rather than add a new drawing path. Road type colors only apply when a road is "detailed" (within `RoadDrawPlanner.DefaultDetailDistance = 150` m `× unitsPerMeter` of the camera); farther/non-detailed roads always draw gray, and direction arrows only draw on one-way roads, per the design's per-distance/per-direction rules. The type filter itself is **not** persisted through `NavigationEditorPrefs` (S23's list of prefs to add didn't include it) — it's plain session state on `NavigationWindow`. The View foldout's toggles wrap in `EditorGUI.BeginChangeCheck`/`EndChangeCheck` and call `SceneView.RepaintAll()` on change so edits show immediately without moving the mouse over the Scene view — later window controls that affect Scene view drawing should do the same. Added a dev-only `Tools > Gley > Navigation Dev > Add Test Roads To Map` menu (`TestMapBuilder.AddTestRoadsToTestMap`, not part of the plan) that seeds the existing `DevTestMap` authoring asset with ~12 generated roads (via a new `AuthoringFromBuildInput.Fill(authoring, input)`, extracted from `Convert` which now delegates to it) and forces every third road one-way, since there's still no in-window way to draw roads before S25; useful for later steps' manual checks too.

- S23: `NavigationAssetLocator.CreateMapAssets` only wires the three links the plan named (authoring→runtime, authoring→map, map→runtime); it does **not** assign `RoadNetworkAuthoring.Settings`. Added `internal RoadNetworkAuthoring.SetMapAsset` (same pattern as `SetRuntimeAsset`) since nothing existed to set that link. Because `Settings` stays null after creation, `NavigationWindow` now resolves/creates the project settings via `NavigationAssetLocator.FindOrCreateSettings()` and assigns it onto the current authoring asset (via `SerializedProperty`, same pattern as `RoadBakerTests`) the first time it finds one with `Settings == null` — this is required for `BakeStatus.IsOutdated` (and eventually `RoadBaker.Bake`) to not NRE on a freshly created map; do the same in any other code path that can reach a brand-new authoring asset before the window has opened once. `NavigationWindow`'s toolbar needs `IRoadEditorMode` plus five stub classes (`DrawMode`, `EditMode`, `ConnectMode`, `ValidateMode`, `BakeMode`) to exist now — the phase file lists these as new files in S25–S27, but they already exist as empty panels from this step; S25–S27 should **change** them, not create them. `TargetMap`'s authoring asset is found via `NavigationAssetLocator.FindAuthoringFor(targetMap.MapData.RoadNetwork)`, which only works because `CreateMapAssets` points `MapData.RoadNetwork` and `RoadNetworkAuthoring.RuntimeAsset` at the *same* runtime instance from creation (even pre-bake) — don't repoint one without the other. Added a dev-only `Assets/Tests/NavigationSystem/Dev/Editor/TestMapBuilder.cs` (menu **Tools > Gley > Navigation Dev > Create Test Map**, not part of the plan) since there is still no in-window "create a map" flow before the Setup window (S54); it creates a linked map/authoring/runtime set under `Assets/NavigationData/DevTestMap/` and adds a wired-up `NavigationMap` to the open scene, for exercising S22/S23 manual checks. `NavigationAssetLocatorTests` for `FindOrCreateSettings_NoneExists_CreatesWithDefaults` needs the project to have zero `NavigationSettings` assets anywhere (the search is project-wide, not folder-scoped); its `SetUp`/`TearDown` now back up and temporarily delete/restore (byte + `.meta` round-trip, preserving the GUID) any real one it finds so the test stays deterministic regardless of whether the window has been opened.

- S25: Modes get their data from a new `Editor/Tools/RoadEditorContext.cs` (authoring asset, `MapData`, `Prefs`, the window's shared `ValidationIssues` list, `UnitsPerMeter`), built in `NavigationWindow.OnEnable` and refreshed by `UpdateEditorContext()` in `ResolveTarget`. S26/S27 modes should take the same context in their constructor (`new EditMode(editorContext)`) instead of new wiring; after each operation call `ValidationIssues.Clear()` + `RoadValidator.RunCheapChecks` so the Scene drawer shows the result. `SnapTarget` (readonly struct + `SnapTargetKind` None/Intersection/Road, same file) and `SnapFinder` measure **3D** distance on the Bézier curve (not the dense points), so bridges above never snap; `ignoreRoadId` also ignores that road's two end intersections, and a hit at a road's very start/end is returned as an `Intersection` target. DrawMode connects ends *after* creating the road: `CreateRoad(..., 0, 0)`, then each snapped end is re-found with `FindSnap` (5 cm tolerance, ignoring the new road) and joined via `ConnectIntersections`/`ConnectToRoadMiddle` — this stays correct when both ends split the same road; one `Undo.RecordObject` covers it all. Clicking a snap target after the first point finishes the road. Extend mode is one undo step per click. **Right-click:** `EventType.ContextClick` never arrived in the Scene view (2022.3); right-click is detected via `Event.rawType` MouseDown/MouseUp with button 1 and ≤4 px movement (so RMB fly-camera drags still work), and `ContextClick` is only `Use()`d while drawing to suppress Unity's menu — reuse this pattern in S26/S27. Added dev-only `Dev/Editor/SandboxMaterials.cs` (menu **Tools > Gley > Navigation Dev > Apply Sandbox Materials**, also applied by Create Sandbox Scene; not by `BuildCityObjects`, so tests don't write assets) with colored URP Lit materials in `Assets/Tests/NavigationSystem/Dev/Materials/`.

- S26: Handle drags record undo with `Undo.RegisterCompleteObjectUndo` once, on the **first actual change** of the drag (not `RecordObject` at press — `RecordObject` diffs at flush time, so later drag steps would not undo/redo correctly); handle presses are detected by comparing `GUIUtility.hotControl` before/after `Handles.FreeMoveHandle(position, size, snap, cap)` (no control-ID overload used), and the drag end (hotControl released) runs cheap validation. Handle control IDs are ordinal, so `EditMode.DrawHandles` always draws in a fixed order: inner key points of selected roads → end intersections of selected roads + selected intersection → curve handles of the selected inner key point last. Curve handles are **mirrored** (user request): dragging one calls `MoveHandle` for the opposite side with `-offset` too, and the dragged tip stays at its height (projected on a horizontal plane). Picking priority within 10 GUI px: intersection > inner key point > road (own GUI-space segment distance, skipping points behind the camera); split/insert `t` clamped to [0.01, 0.99]. Unity's `SoftDelete`/`Delete` commands are swallowed in the Scene view while the Edit selection is non-empty, so Delete never deletes the selected map GameObject. `SelectionHit` + `SelectionHitKind` live in `Editor/Tools/SelectionHit.cs`. ConnectMode picks via ground raycast + `SnapFinder` (same as DrawMode). Box select ignores the window's type filter (not in `RoadEditorContext`).

- S27: `NavigationSettings.BlockBuildOnProblems` has no window toggle yet — that's S28's "Block build on problems toggle" in the Settings panel. Manual check 6 (block-on-problems build test) was **not run**; it needs S28's toggle (or flipping the serialized field via the Inspector's Debug view) to exercise. All other manual checks (Validate list + frame/highlight, Bake clears outdated, grid cell size, Play-mode warning) and both automated test files passed 2026-09-21.

- S29: Diagnostics-line timing in `NavigationWindow` is `RoadNetworkAuthoring.Version`-diff based (compares before/after each mode's `OnWindowGUI`/`OnSceneGUI` call) plus a 3-frame follow-up window (`RecordOperationTiming`) to catch `EditMode`'s deferred `FinishOperation()` cost after a handle-drag release — `UpdateHandleDragEnd` detects the release on the frame *after* mouse-up, not the release frame itself, so a plain single-frame version check undercounts any drag-then-release gesture (measured as low as 0.4 ms before the fix, vs. ~1.15 ms after). Later steps adding editor-timing diagnostics should reuse that pattern rather than a bare single-frame check. `Assets/Tests/NavigationSystem/Dev/Editor/LargeNetworkGenerator.cs` (menu **Tools > Gley > Navigation Dev > Generate 5k Road Map**) builds `Assets/NavigationData/Dev5k/` from `TestCityGenerator.Generate(5000, 1)` via the existing `AuthoringFromBuildInput.Fill` (no resample, no ground probe, per the S19 note); `TestCityGenerator` always assigns road type IDs 1-4 regardless of the live `NavigationSettings`, so a clean bake needs the project's road types to still include IDs 1-4.
- S30: `RoundUpToMultipleOf4` is shared between rounding the requested `longerSidePixels` and computing the shorter side's pixel count (`ceil(value/4)*4` on the raw float ratio) — both follow the same formula per the design's worked example. `CapturePiece.OrthoSize` and `RenderWidthPx`/`RenderHeightPx` are derived from the **render** rect (with overlap), not the output rect, since the executor (S31) renders at render-rect size; piece pixel size is clamped to `4096 − 2·overlapPx` so the render rect (output + overlap on each side) never exceeds 4096. No asmdef changes needed — `Gley.NavigationSystem.Tests.Editor` already referenced `Gley.NavigationSystem.Editor`.
- S28: `NavigationSettings.SetRoadTypeName`/`SetRoadTypeColor`/`SetChannelName`/`SetUnitsPerMeter`/`SetImperialUnits`/`SetBlockBuildOnProblems` do **not** bump `version` — only `SetRoadTypeSpeed`/`SetRoadTypeWidth`/`AddRoadType`/`RemoveRoadType` do, since `version` only drives `BakeStatus.IsOutdated` and only speed/width/add/remove actually feed baked `RoadRecord` data (confirmed via `RoadNetworkBuilder`); later steps adding more `NavigationSettings` mutators should bump `version` only if the field is actually baked. The "delete a road type in use" dialog is implemented as an inline panel in `SettingsPanel` (HelpBox + Popup + Confirm/Cancel), not a modal `EditorUtility.DisplayDialog`, since the stock dialog can't embed a dropdown. `SettingsPanel` finds all maps to check/reassign via `AssetDatabase.FindAssets("t:RoadNetworkAuthoring")` project-wide (same approach as `NavigationBuildPreprocessor`in `BuildCheck.cs`) rather than through `NavigationAssetLocator`, which has no "find all" method. `NavigationWindow.OnGUI` had no scroll view at all before this step; with the Settings foldout's road type list, content routinely exceeds the window height and pushes the current mode's panel out of view. Added a `scrollPosition` field and wrapped everything below the header/toolbar (View foldout, Settings foldout, mode panel) in an `EditorGUILayout.BeginScrollView`/`EndScrollView` — later steps that add more window content should stay inside that scroll view rather than adding a second one.
- S33: `VehicleMotion.TeleportDistance`/`StoppedSpeed`/`MinHeadingSpeed` are public settable properties with the design defaults set in the constructor (same pattern as `RouteRequest`'s snap-distance properties from S11), not consts — S37 should assign the Manager's serialized fields onto these after construction rather than re-deriving the defaults. `Reset` and the teleport path both flatten/validate the nose heading through the same private `FlattenHeading` helper (fallback to the previous `NoseHeading`, which is `Vector3.zero` until the first valid nose is seen).
- S32: `MapImagePanel.DrawCustomImageField` binds the object field to `data.Image` (not a discarded local) so it keeps showing the current assignment on later repaints — passing a constant `null` as the field's current value, as an earlier draft did, makes it always render empty right after `Assign`. `MapImageImportSettings.GetWarnings`'s "Non Power of 2 scaling is on" check only fires when the image's width or height isn't already a power of two — Unity's own Inspector disables/ignores `npotScale` once the source is POT, but the serialized field still defaults to `ToNearest` (not `None`), so an unconditional check produced an unfixable false-positive warning on every POT custom image (e.g. a 2048×2048 exported template) with no way in the Inspector to clear it. `TemplateExporter.Export` builds its `MapFrame` from `size.AdjustedRectSize`, matching `MapCaptureExecutor.RenderPixels`'s existing pattern, not `data.CreateFrame()`.
- S31: `ICapturePipelineAdapter` got an extra `bool SupportsExposure { get; }` (only HDRP returns true) so the panel can show the exposure field without type-name checks. `CaptureSettings` also holds `pieceSizeMeters` (200), `pieceOverlapPx` (16) and a persisted `hdrpExposureEdited` flag; it's stored as JSON in `NavigationEditorPrefs.CaptureSettingsJson`. `MapCaptureExecutor` centers each piece's camera on the piece's **render rect** (with overlap), not `CapturePiece.CenterMap` (output rect), since edge pieces have one-sided overlap. `MapCaptureExecutor.GetImagePath(data)` derives `<Name>_MapImage.png` by stripping a trailing `_Map` from the map asset name — S32's template export can reuse the same folder logic. With no renderer overlapping the rectangle, camera depth falls back to `RectangleCenter.y`. Preview/Capture run via `EditorApplication.delayCall`, not inside `OnGUI`. The URP/HDRP asmdefs use an empty `versionDefines` expression (works in 2022.3 URP). **UI change (user request):** the Navigation window has a new first toolbar tab **Map** (`Editor/Tools/MapMode.cs`) where no road tool takes Scene clicks; it draws the rectangle corner handles (skipped while the map object itself is selected, whose inspector draws them) and hosts `MapImagePanel` — there is no separate "Map Image" foldout. Mode indices shifted by one (Draw=1 … Bake=5); the window opens in Map mode. Corner-handle code moved from `NavigationMapEditor` into shared `Editor/Tools/MapRectangleHandles.cs` (`Draw(data, worldY, unitsPerMeter)`). S32's custom-image UI should go into `MapImagePanel` (shown in Map mode).
- S34: `RoadMatcher(RoadNetworkData)` builds its own `RoadQuery`; `LeaveMargin`/`UnconnectedSwitchDistance` are settable properties with defaults (S37 assigns the Manager's `leaveMargin` onto `LeaveMargin`); lost radius 30, continuity interval/radius 10/20, better-margin 2 and max 3 fork candidates are consts. One-way roads score heading with the signed dot (`10*(1-dot)`), two-way with `|dot|` (plan question, user chose A). A stopped car that is lost runs the lost search once (so a car parked on a road at start gets matched), then waits until it moves. Road-end pass is checked before the leave threshold (fast cars at low FPS still continue onto connected roads). Continuity: every 10 m a 20 m search picks one unconnected suspect road (must be within its half width and score better by >2); that road is then re-projected every update, accumulating driven distance, switching after 10 m or clearing when no longer better. `ChangedRoad` fires on any on-road road change, including the best fork candidate changing; a teleport sets `EnteredRoad` only if the lost search finds a road. Extra read-only `IsInFork`/`ForkCandidateCount`. `RoadMatcher` computes driven distance itself from successive positions (not passed in). `TestNetworks` gained `Fork`, `ParallelRoads` (11 points/road), `Crossing` builders with explicit width overrides.

## Decisions

Decisions the user makes during implementation (decision points S04, S29, S56, or answers to plan questions), with the date. Format: `YYYY-MM-DD Sxx: <decision>`.

- 2026-09-21 S04: Route line shader widens the line in canvas space using a per-graphic `_CanvasOffsetMatrix` updated on `Canvas.willRenderCanvases` (option B), instead of offsetting in graphic-local meters. Same performance as the sub-canvas-only approach and keeps the "one canvas, no sub-canvases" fallback viable. Nested-canvas clipping passed manual check 2, so the fallback is not needed; S05 keeps `useOwnCanvas = true`.
- 2026-09-21 S29: No chunking for now. On a 5k-road (5176) network: point-only edits (Move Key Point) measured 1.15 ms and Scene view redraw measured 1.78-2.00 ms across zoom levels — both well within budget (≤50 ms, ≤16 ms). Structural edits (Split Road, and by the same `RoadEditOperations.FinishRoads`/`Undo.RecordObject`-on-the-whole-asset path: Merge/Disconnect/Connect/Delete Road) measured 387 ms, ~7.7x over the 50 ms target, with Undo/Redo paying the same cost again. Decided to ship without chunking and revisit with the profiler against real project data once the rest of the system is built, rather than design chunking now against a synthetic stress-test map.
- 2026-09-22 S34: Road-matching score uses the signed heading dot on one-way roads (`10*(1-dot)`), `|dot|` on two-way roads, so antiparallel one-ways are distinguished by heading.
- 2026-09-22 S44: Heading-up minimap rotates to the matched road's direction (oriented toward the nose, flip hysteresis at dot < -0.25), not the raw nose, so weaving in the lane doesn't rotate the map (Google Maps style). Off road it falls back to the nose heading with a 3° dead zone. `RoadMatcher.RoadTangent` + `NavigationManager.RoadHeading`/`HasRoadHeading` (internal) added for this. Road-to-road turns use a longer `turnSmoothing` (0.8 s) whenever the heading target jumps by more than 20° in one frame, until the view is within 2° of it.

## Implementation notes (continued)

- S36: `RerouteDecider.Decide` checks `BackOnRoad` (`matcher.EnteredRoad && session.WrongTurn`) **before** the generic `session.WrongTurn → WrongTurn` check, not after as the plan bullet order suggests — a strict top-to-bottom read would make `BackOnRoad` unreachable, since `session.WrongTurn` is already true in the same tick whenever a freshly-entered road isn't on the route (current/look-ahead). Added `NavigationSession.IsRoadOnRoute(roadIndex, movingForward)` (full-route scan, same matching rule as `JumpTo`) for the teleport check specifically, since "on the route" for a teleport must agree with what `JumpTo` can find anywhere on the route (including backward), unlike `BackOnRoad`'s narrower current/look-ahead window (`session.WrongTurn`). `DistanceSinceLastReroute`/`OnRerouted` follow the plan; added `AccumulateDrivenDistance(float)` (name not in the plan) as the method the Manager (S37/S38) should call each tick to feed driven distance in, separately from `Decide`.
- S35: `NavigationSession.ArrivalDistance` is a settable property (default 10 m, same pattern as `VehicleMotion`/`RoadMatcher`'s settings) — S37 should assign the Manager's serialized `arrivalDistance` onto it after construction. The 30 m look-ahead distance used when advancing across short segments is a private const (`LookAheadDistance`), not exposed as a setting — it isn't in the design's default-value table. `UpdateNavigationSessionLogic` guards on `route == null || route.Segments.Count == 0` (a `Route` with `ArrivedImmediately == true` has zero segments) so the Manager can safely call it even before deciding whether to call `Start` on such a route. `JumpTo` also recomputes `RemainingDistance`/`Eta` (via the same private `UpdateRemainingAndEta` used by the per-tick update), not just `ProgressDistance`/`CurrentSegment`, so callers don't need to call anything else after a jump. Progress within a segment is always clamped to `[0, segment length]` (`Mathf.Abs(DistanceAlong - segment.FromDistance)` clamped), so `RemainingDistance` reaches exactly 0 whenever the car has physically passed the route's last segment — the arrival rule's "passed" and "within distance" branches both fire at that point; they're still functionally distinct because "within distance" also fires *before* the car reaches the end.
- S40: `NavigationEvents`'s 14 `UnityEvent`/`UnityEvent<T>` fields are given inline field initializers (`= new UnityEvent()`, etc.) — without them, a component added at runtime via `AddComponent` (as in the tests, or any code-built rig) has null event fields and `Invoke()` NREs, since Unity's serialization defaults only apply to scene/prefab-loaded instances. Needed three small `[Serializable]` `UnityEvent<T>` subclasses (`FailureReasonUnityEvent`, `StopReasonUnityEvent`, `RerouteReasonUnityEvent`) in the same file since Unity can't serialize the open generic directly. Manual verification surfaced (not a bug, just a test-setup trap for later manual checks): `Arrived` only fires while the car is on-road within `Arrival Distance` of the route's on-road end — a destination placed off-road (design's "dotted piece") freezes `RemainingDistance` the moment the car leaves the road to reach it (S35 behavior), so a target more than `Arrival Distance` from the nearest road never arrives; keep manual-check destinations on or very close to a road.
- S42: `MapView.ComputePose()` calls `MapViewMath.ComputeContainerPose(centerMap, rotationDegrees, CanvasUnitsPerMeter, Vector2.zero)` — the pivot point (where `CenterMap` lands in the viewport) is hard-coded to `Vector2.zero` (the viewport's own local pivot/origin); there is no field or setter for it yet. S44 (Follow Car, "car sits at 30% from bottom") will need to add one. `CanvasUnitsPerMeter` is a live computed property (`viewport.rect.width / zoomMeters`), not cached per frame, so it's always current without extra bookkeeping. `SetZoomMeters(float meters, float maxZoomMeters)` takes the max as an explicit second parameter (not in the plan's signature list) since the plan requires clamping against "the max zoom-out passed by the behavior" — S44/S49 should pass their computed max each time they call it. `MapView`'s container (`Content`) is positioned every frame via `RectTransform.localPosition` (plain `Transform` API) rather than `anchoredPosition`, since `localPosition == (0,0,0)` always coincides with the parent's pivot point regardless of the parent's own pivot fraction or anchors — later UI code in this view that needs the same "map-meters local space" convention should reuse this approach rather than reasoning about anchors. `RouteLineRenderer` is instantiated under `Content` (unconfigured, per the plan) and only ever has `SetLine` called with empty lists (to clear it) — S43 owns giving it a style/shader and real route data.
- S37: Test-only internal setters on `NavigationManager`: `SetSettings`, `SetExplicitMap`, `SetCarReference(car, yawOffset)` (named to avoid clashing with S38's public `SetCar`), `SetStartManually`, `SetShiftSource` — call them right after `AddComponent`, before `Start`. Extra internal read-only `CarTeleported` and `LastRerouteDecision` (what `RerouteDecider.Decide` returned this tick in `UpdateRouteLogic`, guarded by a `hasActiveRoute` flag that S38 must set/clear; S38 acts on the decision). `UpdateNavigationLogic` is private. Map activation creates `RoadQuery`, `Pathfinder`, `RoadMatcher`, `NavigationSession`, `RerouteDecider` and three `Route`s (`activeRoute`/`previewRoute`/`scratchRoute`, converter set); `RoutePreferences preferences` and `RouteRequest carRequest` (snap/arrival distances from the serialized fields) are created once in `Initialize` — S38 should use these, not new ones (they currently cause CS0414 "never used" warnings). A map with no `MapData` is not activated (`LogError`); a map whose road network is null/empty becomes `ActiveMap` but tracking/routing objects stay null (`LogWarning`). `IsOffRoad` is false when there's no car or no road data. `NavigationMap` got a serialized `manager` field, `internal SetMapData` and `internal AttachManager` (the Manager calls it on registration so maps enabled before the Manager still unregister in `OnDisable`). `chosenMap` is cleared when that map unregisters; S38's `SetMap` should set `chosenMap` then call the private `ApplyMapChoice`/`ActivateMap`. Test gotcha: `Time.timeScale = 0` only affects `Time.deltaTime` from the next frame, so yield one frame after setting it.
- S37 (side fix, 2026-09-22): `MapData.EditTimeWorldPosition` is now kept in sync on every editor path — `MapRectangleSync.SnapObjectToAsset` writes it (returns `bool` changed), new `SnapSceneObjectsToAsset(data, unitsPerMeter, undoName)` snaps every scene `NavigationMap` using the asset (with Undo) and is called after corner resizes and inspector size/rotation edits; the locked-revert path writes it too. Any new code that creates a map object (e.g. S54 Setup window) must call `SnapObjectToAsset`, otherwise Rectangle-mode floating origin sees a false shift.
- S38: Every public state-changing Manager call (`SetMap`, `SetCar`, preview/start/confirm/cancel/stop, preference setters) goes through `RunOrQueue`: queued as a `NavigationCommand` struct when `dispatchDepth > 0`, otherwise run inside its own dispatch scope (`BeginDispatch`/`EndDispatch`); `LateUpdate`, `Initialize` and map register/unregister are dispatch scopes too, so handler calls always apply after the whole operation's events. `EndDispatch` at depth 0 drains `CommandQueue.Process` (max 3 rounds, then `LogError` + drop) guarded by `processingQueue` against recursion. Every event is raised through a private `Raise<Event>` wrapper that does the depth bookkeeping — new events (e.g. S46+) must use the same pattern. `MapChanged` fires on **any** active-map change (automatic choice/unregister too), after `NavigationStopped(MapChanged)`/`PreviewCanceled` from `DeactivateMap`. Destinations are stored in True space (`activeDestination`/`previewDestination`); car routes are always computed into `scratchRoute` then swapped with `activeRoute`/`previewRoute`. Route heading for car requests = nose while `carNeedsReset` (new car/map) or motion heading is zero. Preference changes are applied at the end of `UpdateNavigationLogic` even when `deltaTime == 0` (paused). A new failed `PreviewDestination` clears the old preview (no `PreviewCanceled`); a failed while-driving recompute keeps it. `ArrivedImmediately` on start/confirm/reroute → `Arrived` (active cleared, no `NavigationStarted`); on preview → `PreviewReady` with an empty route. `preferences`/`carRequest`/`generalRequest` are readonly fields (not created in `Initialize`); `Initialize` only uses `explicitMap` if `SetMap` wasn't called first. Known quirks: `RerouteDecider.DistanceSinceLastReroute` starts at 0 per map activation, so the very first route after activation has a 20 m cooldown before a wrong-turn reroute; a car turning sharply exactly at an intersection center on a wide road leaves/re-enters via lost search and reroutes as `BackOnRoad` instead of `WrongTurn` (test drives 5 m past the center); `Route.Destination` is still True space (not converted to world).
- S44: `MapViewFollowCar` runs at `[DefaultExecutionOrder(99)]` (before `MapView` at 100) and sets the view's center/rotation/zoom; the car offset is done by shifting the center (general for any viewport pivot), so `MapView` still has no pivot setter. `MapView` gained `internal Manager` and `internal Viewport`. Heading-up rotation = `+HeadingToMapAngle(...)` (clockwise map angle = counter-clockwise container rotation), not "minus" as the plan wrote. Extra serialized fields beyond the plan: `zoomSmoothing` (0.5 s), `fixedZoomMeters` (300, used when speed zoom is off), `noseDeadZoneDegrees`, `turnSmoothing`, `turnAngleThreshold` (see Decisions). Rotation/zoom snap (no smoothing) on the first frame and whenever the active map or car changes. `internal bool PinPlayerToEdge` (= `manager.IsOutsideMap`) is for S47's marker layer. Test gotcha: first-frame SmoothDamp movement depends on real unscaled frame time, so smoothing tests disable the component and call `UpdateFollowCarVisuals(0.1f)` manually. Dev tools added: **Tools > Gley > Navigation Dev > Create Full Sandbox (everything)** (`FullSandboxBuilder`: scene, map, sandbox roads, bake, capture, Manager, minimap, `DevNavigationTester` click-to-navigate) and `DevMinimapCarMarker` (temporary red car marker on the minimap until S47 — delete then).
- S45: `CompassButton` is `[DefaultExecutionOrder(101)]` and calls `transform.SetAsLastSibling()` in `OnEnable` — `MapView` (order 100) builds `Background`/`Content`/`Markers` at runtime in its own `OnEnable`, so a compass icon living under the minimap's viewport GameObject needs to both run after that and re-assert its sibling position on every enable to render on top of them; placing it outside the minimap's hierarchy instead avoids the ordering issue but loses anchoring relative to the mask, and breaks the `MapView`/`MapViewFollowCar` auto-resolution (`GetComponentInParent`) unless wired manually. `MinimapShape` tracks a hidden serialized `ownsImageComponent` flag so switching Rectangle → Sprite → Rectangle removes the `Image` it created for the stencil, not just the `Mask`, otherwise a leftover opaque `Image` renders as a solid block once its `Mask` is gone; `Application.isPlaying` selects `Destroy` (deferred a frame) vs `DestroyImmediate` for both this and the Mask/RectMask2D swap. `MinimapTapToOpen` needs no `Graphic`/`RequireComponent` of its own: uGUI's click resolution (`ExecuteEvents.GetEventHandler`) walks up the parent chain from whatever raycast target was hit, so placing it on the viewport GameObject (parent of `Background`, the only raycast target in the view) is enough to receive taps.
- S43 (side fix, 2026-09-22): uGUI silently drops a `CanvasRenderer` mesh whose vertex bounds have zero width or height (`absoluteDepth` stays -1 or no draw call appears), even though the shader would widen it. `RouteLineMeshBuilder` used to stack each left/right vertex pair on the centre point, so perfectly vertical or horizontal routes rendered nothing. `AddVertexPair` now pushes the pair apart by `BoundsPadding` (0.05 map units) along the offset normal; the shader still adds the full width on top. Any future custom `Graphic` that widens in the shader must keep non-degenerate vertex bounds. Follow-up fix (same day): `RouteLineGraphic.TryComputeCanvasOffsetMatrix` used the graphic-to-batch-canvas rotation times rootScale/batchScale, which was wrong when the nested canvas sits under a scaled/rotated parent (MapView's `Content`): the line was `1/contentScale` too thick and ignored `Content`'s rotation. `RouteLineRenderWidthTests` (PlayMode, renders to a RenderTexture and counts pixels) proved nested-canvas vertices reach the shader in root canvas space (nested-below-×0.5: 16 px instead of 8; rotated 90°: 0 px). The matrix is now the graphic-to-root-canvas 2x2 with normalized columns, no scale factor.
